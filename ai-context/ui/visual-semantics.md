# Visual Semantics

Example visual semantics:

* Circle = process
* Diamond = decision
* Blue glow = running
* Yellow pulse = retry
* Red pulse = failure
* Green edge = success

Visual semantics should remain consistent across the platform.
